BV Hardwares — scrollbar below navbar patch

Changes:
- Viewport scrollbar track begins below the navbar (64px mobile/tablet, 68px desktop)
- Replaces the bright full-height blue rail with a softer premium rail
- Uses a slim rounded indigo/blue thumb inspired by the supplied reference
- Adds a subtle neutral track and hover state
- Nested scroll areas keep their own compact scrollbar and do not inherit the navbar offset

Apply:
Extract this ZIP and merge/replace the folders into the project root.
